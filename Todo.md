# TODO

*This is a temporary file with a rough list of tasks to complete to finish this project.*

[ ] Copy over ERB content (the `Configuration` directory *et al.*), dependencies from SorrellWm
[ ] Create a sub-package for C++ code, add as dependency to main package
[ ] Migrate Build script from SorrellWm, in new directory `Script/Build`
[ ] Add C++ boilerplate and helper content from SorrellWm
[ ] `build-cpp` script in main project that runs build script (*i.e.*, the TypeScript module under `Script`)
[ ] Create a shared sub-package for types shared by main package and cpp package
[ ] Create Documentation as a Vitepress website under the `electron-react-boilerplate-docs` repo
[ ] Add example content (simple page that gets the number of windows via the Windows API, and holds an internal counter that is get/set from the frontend via IPC with main)
